# 开发原型结果档案（PV+BESS，非主线最终结果）

> **用途限制**：以下数字来自当前 PV+BESS 原型：系统太阳形状的本地 PV 场景、PV 标量误差、词典序成本→碳目标，且不含风电、碳预算、有效回放容量或联合风光碳残差。它们仅用于检查原型求解器的代数可行性、敏感性和迁移前的回归对照，**不得**作为已确认“风光储—双侧不确定性—碳预算”主线的论文表格或结论。
>
> **当前主线基准**：风光储—预测碳预算的四窗口确定性结果已写入 `data/results/four_windows_mainline_summary.csv`。它包括预测碳、实际碳、实际预算违反、弃电与实际并网/爬坡违反；但尚未比较联合不确定性方法，不能替代正式主结果表。
>
> **累计柔性包络与公共碳预算重跑（2026-08-26）**：采用固定总工作量、两日循环块、$H=6$ h，并将所有方法的碳预算集合预先固定为 $\{0,2.5\%,5\%\}$。0% 与 2.5% 在四个窗口均可行，因此 2.5% 用作公共主比较情景；5% 在 4/7/10 月可行而 1 月不可行，只作为严格压力情景和可行性边界。不可行情景保留并报告，不通过修改柔性窗口或资产容量规避。事后回放中，1 月和 7 月仍出现实际碳预算违反，说明预测碳约束可行不等于样本外实际碳约束满足，也构成后续 SAA/RO/DRO 比较的风险信号。
>
> **成本口径更新**：本档案中的历史数值均未计入 BESS 吞吐衰减成本；当前代码默认按 20 USD/MWh 累计充放电吞吐量计入，并分列购电成本、BESS 衰减成本和调度相关运行成本。风光与储能固定运维成本及年化投资成本在主比较中因资产配置相同而未纳入目标函数或成本降低率。鲁棒保护项仅作为算法目标构成单列。因此涉及 BESS 的旧成本下降率不能与后续重跑结果直接比较。
>
> 主线完成后，以相同资产配置重跑确定性、SAA、静态 Γ-RO 与所提联合方法；正式结果应按 [docs/model.md](model.md) 的实际风光/实际碳回放口径生成。

## 1. 算力侧 workload 特征（Alibaba cluster-trace-v2018，8 天）

| 指标 | 值 |
| --- | --- |
| 批处理任务数 | 14,295,731 |
| 实例数 | ≈ 1.40×10⁹ |
| 作业数（job） | 4,201,014 |
| `task_type` 类别 | 12 类；type=1 占 85.4% |
| DAG 结构 | type=1 ↔ DAG（`task_name` 为 M/R/J+数字）；其余为独立任务 |
| 任务时长 | 中位 <1 分钟，p95 ≈ 6 分钟，均值 ≈ 90 秒 |
| 可延迟能量占比（静态代理） | ≈ 58.7%（core-seconds 口径） |
| 在线静态预留占比 | 93.5%（362,072 / 387,264 核） |

## 2. 算力侧不确定集

> 本节是旧 Poisson/总能量波动原型的开发记录。主线已改为固定总 core-hour 的两日块重采样与独立柔性窗口，不再使用下表作为算力场景标定结果。

| 量 | 名义 | 波动 |
| --- | --- | --- |
| 到达率（任务/小时） | 71,838 | std 37,481；min 1，max 182,753 |
| 30 天批处理总能量相对不确定度 δ | — | ≈ 7.9%（由 8 天日能量 CV 0.43 / √30 估计） |

## 3. 功率模型场景（无遥测，按约 96 核服务器区间设定）

| 场景 | PUE | 空闲功率 | 活动功率/核 |
| --- | --- | --- | --- |
| low | 1.10 | 100 W | 2.0 W |
| base | 1.20 | 150 W | 3.0 W |
| high | 1.40 | 200 W | 4.0 W |

base 场景：`P_must ≈ 2.03 MW`（在线+基座），`P_peak ≈ 6.73 MW`（基线峰值）。

## 4. 确定性调度结果（Jan 2025，base 功率场景）

### 4.1 批处理能量平移（无并网/爬坡/BESS/PV）

| 口径 | 值 |
| --- | --- |
| 基线总购电成本 | 83,934.41 |
| 平移后总购电成本 | 61,699.31 |
| 总成本下降 | 26.49%（仅批处理部分为 55.15%） |

### 4.2 并网上限 G_max（归一化扫参，无爬坡）

| G_max / P_peak | 可行 | 总成本下降 |
| --- | --- | --- |
| 0.5 | 否 | — |
| 0.6 | 是 | 7.4% |
| 0.8 | 是 | 18.0% |
| 1.0 | 是 | 21.9% |
| 1.2 | 是 | 24.4% |

### 4.3 爬坡 R_max（固定 G_max=1.0×峰值）

| R_max / P_peak | 可行 | 总成本下降 |
| --- | --- | --- |
| 0.02 | 否 | — |
| 0.05 | 是 | 18.3% |
| 0.1 | 是 | 19.8% |
| 0.5 | 是 | 21.7% |
| 1.0 | 是 | 21.9% |

### 4.4 BESS（固定 G_max=0.8×峰值、R_max=0.1×峰值/h，能量=2h×功率）

| BESS 功率 / P_peak | 总成本下降 |
| --- | --- |
| 0 | 16.5% |
| 0.10 | 17.5% |
| 0.25 | 18.4% |
| 0.50 | 19.4% |
| 0.75 | 19.9% |
| 1.00 | 20.2% |

### 4.5 本地 PV（系统太阳形状 × 场景容量，固定 G_max=0.8×峰值、R_max=0.1×峰值/h）

无 BESS：

| PV / P_must | 可行 | 总成本下降 |
| --- | --- | --- |
| 0.0 | 是 | 16.5% |
| 0.5 | 是 | 24.0% |
| ≥1.0 | 否（爬坡约束） | — |

加 BESS（0.5×峰值、2h）：

| PV / P_must | 可行 | 总成本下降 |
| --- | --- | --- |
| 0.0 | 是 | 19.4% |
| 0.5 | 是 | 27.4% |
| 1.0 | 是 | 35.2% |
| 1.5 | 是 | 42.5% |
| 2.0 | 否 | — |

## 5. 双侧 Γ-budget RO

### 5.1 算力侧（批处理总能量不确定，δ≈7.9%）

固定 G_max=0.8×峰值、R_max=0.1×峰值/h、BESS 0.5×峰值/2h、PV 1.0×P_must：

| Γ | 含义 | 总成本下降 |
| --- | --- | --- |
| 0.00 | 名义 | 35.2% |
| 0.25 | 轻度鲁棒 | 34.3% |
| 0.50 | 中度 | 33.4% |
| 0.75 | 较强 | 32.5% |
| 1.00 | 完全鲁棒 | 31.6% |

### 5.2 能源侧（PV 残差不确定，ε≈24.3%）

固定 G_max=0.8×峰值、R_max=0.1×峰值/h、BESS 0.5×峰值/2h、PV 1.0×P_must：

| Γ_pv | 含义 | 总成本下降 |
| --- | --- | --- |
| 0.00 | 名义 PV | 35.2% |
| 0.25 | 轻度鲁棒 | 34.2% |
| 0.50 | 中度 | 33.3% |
| 0.75 | 较强 | 32.4% |
| 1.00 | 完全鲁棒（PV 降 24.3%） | 31.4% |

## 6. 样本外回测（可靠性验证）

用 400 个随机实现验证：名义方案（Γ=0）和鲁棒方案（Γ>0）在“批处理总能量超预期”和“PV 出力低于预测”两种不确定下，违约率与收益。固定 §7 主实验配置（G_max=1.0×峰值、R_max=0.1×峰值/h、BESS 0.5×峰值/2h、PV 1.0×P_must）。

| Γ = Γ_pv | 总成本下降 | 批处理违约率 | 并网越限率 |
| --- | --- | --- | --- |
| 0.00 | 38.3% | 48.5% | 100% |
| 0.25 | 36.7% | 39.5% | 76.0% |
| 0.50 | 35.0% | 27.5% | 55.8% |
| 0.75 | 33.3% | 21.5% | 29.0% |
| 1.00 | 31.6% | 15.5% | 0% |

结论：

- 名义方案（Γ=0）把并网用到满负荷、零裕量，因此任何 PV 缺口都会导致并网越限（100%）；批处理也有约 48.5% 概率超过计划。
- 完全鲁棒（Γ=1）把并网越限降到 0%，批处理违约降到 15.5%，代价是成本下降从 38.3% 降到 31.6%，**牺牲约 6.7 个百分点**。
- 这就是“保守性—可靠性折中”的量化：多花 6.7pp 成本，换来“零并网越限 + 批处理违约减少约三分之二”。

图：`docs/figures/fig_backtest_tradeoff.png`（成本下降 vs 违约率，随 Γ 的双轴曲线）。

## 7. 四窗口结果（1/4/7/10 月，主实验配置，词典序 cost→carbon）

固定 G_max=1.0×峰值、R_max=0.1×峰值/h、BESS 0.5×峰值/2h、PV 1.0×P_must。目标为词典序：先 min 成本，再在 1% 成本带内 min 碳。批处理负载是同一份 30 天包络，只有能源侧（电价/碳）随季节变。

| 窗口 | 成本下降 (Γ=0) | 碳下降 (Γ=0) | 尖峰下降 | 爬坡下降 | PV 自用率 |
| --- | --- | --- | --- | --- | --- |
| 1 月 | 37.7% | 23.6% | ≈0% | 77.3% | ≈100% |
| 4 月 | 39.8% | 25.4% | ≈0% | 77.3% | ≈100% |
| 7 月 | 32.3% | 16.5% | ≈0% | 77.3% | ≈100% |
| 10 月 | 38.3% | 23.6% | ≈0% | 77.3% | ≈100% |

结论：

- **成本下降**四窗口 33–40%，方向一致（“四季节方向一致”成立）。
- **碳下降**四窗口 15–25%，方向一致。
- **尖峰几乎不变**：因为 `G_max=P_peak`，且成本最小化会把批处理堆到最便宜时段、用满并网，所以尖峰没降。降尖峰需要收紧 `G_max` 或加尖峰惩罚（成本与尖峰是 tradeoff）。
- **爬坡下降 77%**：爬坡约束绑定在 `R_max=0.1×峰值`，优化后购电曲线被显著平滑。
- **PV 自用率 ≈100%**：主实验 PV 容量只有 `1.0×P_must`，远小于总负荷，所以本地 PV 永远被就地消纳、无弃光；弃光/自用率的 tradeoff 要到更大的 PV（1.5–2.0×P_must，见 §4.5）才显现。

## 8. 对照基线的样本外回测（四窗口，PV 越限率）

每窗口 500 个随机实现（全局 PV 短缺因子 `ξ~U(0, 24.3%)`），比较三类方法。

| 窗口 | 方法 | 成本下降 | PV 越限率 |
| --- | --- | --- | --- |
| 1 月 | 确定性 | 38.3% | 100% |
| 1 月 | SAA（20 场景） | 35.8% | 19.4% |
| 1 月 | 逐小时鲁棒（Γ_pv=72） | 36.7% | 0% |
| 4 月 | 确定性 | 40.4% | 100% |
| 4 月 | SAA（20 场景） | 36.4% | 33.2% |
| 4 月 | 逐小时鲁棒（Γ_pv=72） | 38.7% | 0% |
| 7 月 | 确定性 | 32.9% | 100% |
| 7 月 | SAA（20 场景） | 30.3% | 23.0% |
| 7 月 | 逐小时鲁棒（Γ_pv=72） | 31.8% | 0% |
| 10 月 | 确定性 | 38.9% | 100% |
| 10 月 | SAA（20 场景） | 34.9% | 25.2% |
| 10 月 | 逐小时鲁棒（Γ_pv=72） | 37.3% | 0% |

结论（四季节方向一致）：

- **确定性**在四个窗口都是 **100% 越限**：零裕量，任何 PV 缺口都超限。
- **逐小时预算鲁棒**在四个窗口都是 **0% 越限**：代价是成本下降比确定性低约 1.6–2.4 个百分点。
- **SAA 居中**：越限率 19–33%，成本略低于/高于鲁棒（按平均短缺规划）。

注：SAA 是两阶段方法，严格评估应在第二阶段再调度后计违约；这里用平均购电曲线近似。

## 6. 图表

见 `docs/figures/`（Γ 折中、BESS/PV 敏感性、并网/爬坡）。
