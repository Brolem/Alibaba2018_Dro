# 实现与验证日志

本文只记录代码实现、执行命令、验证证据和已知边界。稳定的数学定义见
[model.md](../research/model.md)，当前能力与后续路线见 [design.md](design.md)，正式实验结果见
[results.md](../research/results.md)。每次实现只在最合适的文档中详细记录，避免同一内容在多个文件重复并逐渐不一致。

## 2026-08-27：SAA 场景与有限追索原型

- 提交：`6b77762 Add SAA scenario calibration and recourse prototype`。
- 生成 `calibration_day_blocks_2024.csv` 和 `saa_scenarios_manifest.json`，固定训练、验证和回放场景的来源、随机种子与输入哈希。
- 将 SAA 接入共同日前模型；固定日前 BESS，允许场景批处理在累计释放/截止包络内有限追索。
- 执行发现：固定批处理与场景包络冲突；改成有限追索后，单体式 `N=20` 联合 MILP 在时限内仍无可行 incumbent。因此没有启动完整三折校准。

## 2026-08-27：活动场景分解预检

- 提交：`b7371ab Add decomposed SAA preflight workflow`。
- 实现活动场景约束生成、风险一致的独立场景回放、单场景零违反恢复、warm start 和 4 进程并行回放。
- `fold_1 / N=20 / 1 window` 两轮预检：3 场景主问题约 81 秒最优；加入 1 条完整场景后，4 场景主问题约 107 秒最优。工作负载、并网和爬坡违反均为 `0/20`，碳违反连续两轮为 `11/20`。
- 结论：逐轮增加一条完整碳场景可解，但碳风险收敛不足；完整校准继续暂停，下一步改为场景 LP 对偶割。

## 2026-08-27：碳排放 LP 对偶割

### 实现

- 新增固定日前 BESS 的场景最小碳排放 LP。变量为场景批处理、购电和弃电；硬约束保留批处理累计包络、总工作量、并网上限和爬坡限制。
- 初版尝试从 PySCIPOpt 读取对偶值，依次遇到：变换后约束为空、旧对偶 API 被当前版本拒绝、求解后 LP 行集合为空。关闭预求解或把约束设为可修改仍不能得到经有限差分验证的对偶值。
- 安装 SciPy 后，改用 `scipy.optimize.linprog(method="highs")` 显式构造同一 LP，并从平衡等式的 `eqlin.marginals` 构造关于日前充/放电功率的支撑切平面。
- SAA 主问题支持全局场景编号和多条碳割；同一场景的所有割共享一个碳违约二进制变量，碳机会约束分母仍为完整样本数。
- 分解循环对当前全部碳违反场景批量生成割；工作负载、并网和爬坡通道仍使用完整活动场景。
- 结果表新增 `carbon_cut_count`，用于审计每次求解实际使用的割数。

### 已完成的小规模验证

```powershell
conda run --no-capture-output -n scip_env python -m unittest tests.test_mainline_scheduler.MainlineSchedulerTests.test_saa_workload_chance_constraint_uses_joint_scenario_envelope -v
```

- 基点割对 `0.01 MW` 充电扰动满足下界性质，且预测值与重新求解的 LP 最优值在 `1e-5` 精度内一致。
- 在碳违约额度为零时，人为构造的不可满足割会使主问题不可行，证明割已实际接入约束系统。

### 预检结果与边界

- 首次命令：`fold_1 / N=20 / 1 window / 3 iterations / 4 workers / 120 s`。第 1 轮 3 场景主问题在 82 秒最优；全场景回放为 workload `0/20`、carbon `11/20`、grid `0/20`、ramp `0/20`，并批量生成 11 条碳割。第 2 轮保持 3 个完整活动场景，但在 120 秒内无可行 incumbent（`mip_gap=1e20`），因此没有执行第 2 次回放。
- 失败分析：首版每条割沿用“全时域最大购电排放”Big-M，该值比支撑平面在 BESS 可行域内的最大值更松；且 120 秒只略高于旧 4 场景主问题的约 107 秒，不能区分数值松弛与真实不可行。
- 改进：利用逐时凸包 $p_t^{ch}+p_t^{dis}\le P^{BESS}$，将割的 Big-M 收紧为 $\max\{0,\alpha_s+P^{BESS}\sum_t\max(0,g_{s,t}^{ch},g_{s,t}^{dis})-\bar E\}$，并输出每批割的 Big-M 范围；在独立目录以 300 秒时限重跑。
- 收紧后 11 条割的 Big-M 为 `197787.168--224285.176 kg`；第 2 轮延长到 300 秒后仍无 incumbent（`mip_gap=1e20`）。因此“仅因 120 秒过短”被排除，单纯继续延长时限不作为下一措施。
- 下一改进：加入碳割后，先解“1 个完整活动场景 + 当前全部碳割 + 原 `2/20` 碳违约额度”的较小恢复主问题，构造满足割的日前 BESS warm start，再进入下一轮完整活动场景主问题。该恢复问题若不可行，也可直接暴露当前割集合与风险额度的冲突。
- 恢复预检中，小恢复模型 38 秒判定不可行，随后完整第 2 轮在 269 秒判定不可行。为量化冲突，新增“先最小化割违约数”的诊断目标。
- 首次下界诊断的小恢复模型 36 秒判定不可行；诊断阶段因目标只含违约二进制量，出现 1 小时同时充放并被互补性审计拒绝，未生成正式结果。改进为无任意权重的两阶段词典序：先固定最小违约数，再以原运行成本（含 BESS 吞吐衰减成本）整理日前计划。
- 最终诊断中，第 1 轮主问题 76 秒最优，回放为 workload `0/20`、carbon `11/20`、grid `0/20`、ramp `0/20`；`2/20` 恢复模型 34 秒判定不可行；两阶段诊断 93 秒最优，得到 `carbon_cut_violation_lower_bound=6`。
- 结论：当前割是场景最小碳值函数的支撑下界，割松弛仍至少需要 `6/20` 个碳违约场景，因此正式允许的 `2/20` 无法满足。完整三折 SAA 校准继续暂停；下一步必须在碳预算收紧率、碳可靠性目标或资产配置之间做显式研究设计选择，不能把求解失败误判为继续加时即可解决。

## 2026-08-28：主目标转为成本与运行可靠性

- 研究目标改为联合算力、风电和光伏不确定性下的调度相关运行成本最小化，并控制算力包络、并网容量和爬坡三类运行风险。
- `scheduler.py` 的确定性和 SAA 求解默认不再添加预测碳预算、场景碳机会约束或碳割；当时场景追索只优先处理并网和爬坡，随后在边界审核中补入可测的算力包络违反；碳排放由最终购电量事后核算。
- 历史碳预算/碳割代码仅由 `enforce_carbon_budget=True` 显式启用，用于复现此前不可行性诊断，不属于当前论文主流程。
- `run_four_windows.py` 移除碳预算收紧率循环和碳违约字段；`run_uncertainty_methods.py` 的样本量选择只使用算力包络、并网和爬坡三类 Wilson 上界，同时继续保存实际碳排放。
- 删除未提交的五个自适应碳预算边界搜索目录；此前已提交的碳割诊断结果作为历史证据保留。
- 本次不启动完整 SAA 校准或四窗口长实验。下一步是在新口径下运行单折单窗口 SAA 短预检。

## 2026-08-28：当前边界计划审核与三风险收口

- 审核确认可保留确定性/SAA/静态 $\Gamma$-RO/TV-DRO 比较、2023--2025 时间隔离、同源种子隔离算力评价和 $2\times2$ 机制消融。
- 优化随机向量收口为算力释放轨迹、风电残差和光伏残差；碳残差继续随能源日块保存，但只用于排放核算，不参与机会约束、$\Gamma$ 压力集、TV-DRO 风险通道或参数门槛。
- 静态 $\Gamma$-RO 的能源压力维度由原计划 `3T` 改为风光 `2T`；原 $\Gamma$ 网格只作候选起点，实施时需用短预检重新检查。
- 场景批处理追索新增算力包络违反二进制量。累计到达/截止包络用与总工作量一致的 Big-M 松弛；风险优先阶段同时最小化算力、并网和爬坡违反，避免“成功时恒为 False、不可行时直接报错”。
- 36 条验证伪窗口必须同时报告违反数、原始率、单侧 95% Wilson 上界和违反幅度。2025 四窗口只支持跨季节情景稳健性，不单独支持年度可靠性概率结论。
- 清理总览、模型、设计、预测、数据可行性、图表计划和结果导航中的现行碳预算表述；历史碳割诊断仍保留为明确标注的过程档案。

## 2026-08-28：算力包络与有效容量文档定版

- 新增 `compute_envelope.md`，集中补全“任务工作量—固定总量释放轨迹—累计释放/到期包络—物理/有效容量—统一容量缩放—MW/MWh 映射—调度约束”的连续推导、代码数据流，并明确数据证据、反事实参数和推导结论的边界。
- 说明在线负载与批处理共同受有效容量约束；代码只显式限制批处理，是因为在线静态预留已作为不可调参数代入共享容量约束。
- 登记当前 $\kappa$ 敏感性的限制：每个 $\kappa$ 都会重新计算统一缩放系数，因此它表示有效系统规模与回放工作量同步变化的容量口径情景，不是固定需求的纯容量稀缺性试验。
- `model.md` 只保留核心约束；将 `design.md` 中重复公式收缩为代码接口与口径约束，根目录索引、`data_feasibility.md` 和文档导航统一链接到 `compute_envelope.md` 的唯一完整推导。本次只修改文档，未修改代码或运行实验。

## 2026-08-28：全量文档边界与重复审核

- 核对仓库内 17 个 Markdown 文件，统一当前目标为成本最小化与算力包络、并网、爬坡三类运行可靠性；碳排放只作环境绩效，碳预算内容只保留在历史档案。
- 修正根目录、代码模块、数据分层、workload 派生和预测文档中的过时状态：SAA manifest、有限追索和活动场景分解已实现，完整三折、静态 $\Gamma$-RO、TV-DRO 和正式 2025 比较尚未完成。
- 删除主模型中重复的碳排放公式，修正场景追索的 core-hour 与 MW/MWh 混用；压缩历史结果页顶部的重复警告，并修正章节编号。
- 进一步明确 $\kappa$ 是有效系统规模与回放工作量同步变化的容量口径情景，不将其误写为固定需求的纯容量收紧敏感性。本次仅优化文档，未修改代码或运行实验。

## 2026-08-29：三风险 SAA 预检通过并启动完整三折

- 启动前发现 `run_uncertainty_methods.py` 的运行配置仍引用已移除导入的碳预算常量，分解协议名称仍指向碳对偶割，样本量选择说明仍写成“四类”风险；若直接运行会在写配置时触发 `NameError`，且元数据与当前研究边界不一致。
- 最小修正：运行配置改为 `active_scenarios_with_scipy_highs_three_risk_replay`，删除碳预算参数，选择规则统一为算力包络、并网和爬坡三类单侧 95% Wilson 上界；分解 SAA 新增默认关闭的 `enforce_carbon_budget` 开关，使碳排放可以继续核算但不再触发活动场景、碳割或可行性门槛。历史碳诊断仍可显式开启该开关复现。
- 回归验证：`tests.test_uncertainty_calibration` 与 `tests.test_mainline_scheduler` 共 10 项通过；新增高碳残差场景检查，确认实际碳排放可高于历史预算参考而三风险 SAA 仍按运行约束返回可行。
- 新口径预检命令固定为 `fold_1 / window=0 / N=20 / 300 s / 3 iterations / 4 workers`。结果位于 `data/results/calibration/saa_three_risk_preflight/`：第 1 轮 3 个活动场景主问题最优，20 条训练回放的 workload/grid/ramp 均为 `0/20`；独立验证窗口三类违反量最大约 `3.98e-13`，状态为 `optimal_decomposed`，总运行时间 451.97 秒。
- 预检通过后启动 `data/results/calibration/saa_three_fold/` 的完整网格：三折、每折 12 个验证窗口、$N\in\{20,50,100,200\}$、单次主问题 900 秒、最多 8 次分解、8 个并行回放进程。逐组合结果追加到 `saa_cv_runs.csv`，相同配置可在中断后跳过已完成组合继续运行；完整汇总与样本量选择只有在 144 个组合全部完成后生成。
- 根据前两个正式组合的实测耗时（`N=20` 为 353.83 秒，`N=50` 为 762.26 秒），线性外推完整三折约需 52--60 小时；若大样本触发额外分解轮次，则可能接近 3 天。该估计只用于运行监控，不替代最终实测时间。
- 删除八个已停止的碳预算/碳割预检结果目录，包括早期单体式、凸包、活动场景、碳割、恢复和下界诊断输出；保留本日志中的必要失败摘要，原文件仍可从 Git 历史恢复。

## 2026-08-29：SAA 样本数改为自适应顺序选择

- 完整网格前两条正式记录显示 `N=20` 与 `N=50` 分别耗时 353.83 秒和 762.26 秒；继续跑满 144 个组合预计需要 52--60 小时，且后续 N 对“最小充分样本量”结论可能没有贡献。
- 停止正在 `fold_1 / window=0 / N=100` 回放的全网格主进程及 8 个子进程；该运行只完成 2/144 条记录，未形成任何候选的 36 窗口汇总，因此删除未提交的 `saa_three_fold/` 配置和结果文件，不与新协议混用。
- `run_uncertainty_methods.py` 默认改为 `selection_mode=adaptive`，候选自动按 $20\rightarrow50\rightarrow100\rightarrow200$ 升序。每个 N 先完整运行三折各 12 个窗口并写检查点；三类单侧 95% Wilson 上界全部不超过 0.10 时立即停止，否则才进入下一个 N。保留 `full_grid` 仅供显式诊断，不作为主线默认。
- 运行配置升级为 `2024_three_fold_sequential_sample_selection_v2`，新增候选顺序、折号、每折窗口数和选择模式，防止用相同输出目录混入不同协议。选择规则不再比较后续 N 的成本，而是选择最小达标 N；若全部 N 均失败，仍按最大 Wilson 上界最小者降级选择。

## 2026-08-29：SCIP/Gurobi 追索求解器基准

- 在 `scip_env` 验证 `gurobipy 13.0.3` 与本机学术许可证可用。`scheduler.py` 新增 Gurobi 三阶段追索后端，与 SCIP 共享算力包络、并网、爬坡、固定日前 BESS、批处理总量和词典序目标；每个 Gurobi 子进程固定 1 线程。`run_uncertainty_methods.py` 新增 `--recourse-solver {scip,gurobi}` 并写入运行配置。
- A/B 基准固定 `fold_1/window=0/N=20`、3 次分解上限、300 秒日前时限和 8 个回放进程，日前主问题均由 SCIP 求解。SCIP 追索总时间 382.11 秒，Gurobi 追索 197.57 秒，加速 1.93 倍、耗时减少 48.3%；两组均为单轮 `optimal_decomposed`，活动场景 3 个，训练三类违反均为 `0/20`。
- 两组名义运行成本完全相同，训练平均批处理调整量差 `1.71e-13 MWh`，验证调整量差 `2.77e-13 MWh`，违反判定一致。验证实际成本相差 0.054%、碳排相差 0.149%，源于第三阶段只固定总购电量而存在多个等价逐时解；它们不是风险或词典序目标差异。正式自适应三折据此采用 Gurobi 追索，完整证据见 `data/results/calibration/saa_solver_benchmark_summary.json`。
- 正式命令使用 `selection_mode=adaptive / N=20,50,100,200 / 3 folds / 12 windows / 900 s / 8 iterations / 8 workers / recourse_solver=gurobi`，输出到 `data/results/calibration/saa_adaptive_three_fold/`。首条 `fold_1/window=0/N=20` 检查点在 196.54 秒完成，单轮最优且训练三类违反均为 `0/20`，随后自动进入下一个窗口。

## 2026-08-29：主线求解器迁移时点确认

- 已确认当前 N=20 自适应阶段不在运行中途更换求解器，继续使用“SCIP 日前主问题 + Gurobi 追索”，保留并续写现有检查点。
- 迁移触发点定为：SAA 自适应样本量选择完成之后、静态 $\Gamma$-RO 与 TV-DRO 实现之前。届时把确定性日前、SAA 日前以及后续 RO/DRO 主问题统一到 Gurobi；已经使用 Gurobi 的追索层保持不变。
- 迁移不是直接删除 SCIP 路径。先用相同窗口、场景、参数和随机种子做 SCIP/Gurobi 等价性核对，至少比较目标值、三类风险判定、关键调度量和约束余量；核对通过后再启用全 Gurobi 主线，并记录求解器版本、线程数和运行时间。
- 当前混合求解器结果继续作为 SAA 参数选择证据；迁移后的正式方法比较使用独立结果目录和运行配置，避免不同求解器阶段的结果被误合并。本条仅记录已批准的后续实施顺序；截至 2026-08-29，该主问题迁移尚未执行。

## 2026-08-30：SAA 自适应三折完成并冻结 N=20

- `data/results/calibration/saa_adaptive_three_fold/saa_cv_runs.csv` 已完成 36 条唯一记录：三个季节平衡折各 12 个验证窗口，全部状态为 `optimal_decomposed`，无缺失、重复或不可行记录。
- $N=20$ 的算力包络、并网容量和爬坡违反均为 `0/36`；三类违反率均为 0，单侧 95% Wilson 上界均为 `0.0699007`，低于预注册的 0.10 门槛。因此 `saa_selection.json` 按“最小达标 N”规则冻结 `selected_sample_size=20`，自适应停止且不再运行 $N=50/100/200$。
- 36 个窗口的平均求解时间为 182.25 秒，累计求解时间为 6560.82 秒；平均名义运行成本为 13128.34 USD，平均实际运行成本为 14492.47 USD。这些成本只描述 SAA 校准样本，不用于提前比较 SAA、RO 与 DRO 的优劣。
- 三类最大违反量分别为 `2.56e-13 MWh`、`0 MW` 和 `1.33e-15 MW`，属于数值容差内残差。运行配置和逐窗口结果的 SHA-256 均与选择文件一致。
- 下一实施门槛转为日前主问题的 Gurobi 迁移与 SCIP/Gurobi 等价性核对；通过前保留 SCIP 路径，且本次混合求解器校准结果不与后续全 Gurobi 正式比较结果混写。

## 2026-08-30：日前主问题迁移到 Gurobi

- `scheduler.py` 增加薄 Gurobi 适配层，使确定性与 SAA 日前主问题复用原有变量、目标和约束表达式；模型创建、状态、时限、warm start 和结果读取按后端转换，不复制数学模型。日前与追索主线默认改为 Gurobi，每个模型固定 1 线程；显式 `scip` 路径继续作为等价性和历史诊断基准。
- `run_four_windows.py` 新增 `--day-ahead-solver`；`run_uncertainty_methods.py` 新增同名参数并写入 schema 3 运行配置，日前与追索参数默认均为 `gurobi`。旧 schema 2 的 SAA 三折结果保持冻结，不用新配置续写。
- 小规模确定性/SAA 单元测试确认两后端名义目标和风险一致。固定 `fold_1/window=0/N=20`、3 个活动场景、Gurobi 追索、8 个回放进程的 A/B 中，SCIP/Gurobi 日前主问题分别耗时 11.00/5.481 秒，Gurobi 降低 50.2%；端到端分别为 69.43/67.51 秒，仅降低 2.78%，说明该窗口总耗时已由追索占主导。
- 两组名义运行成本差 `1.82e-12 USD`，预测购电逐时最大差 `1.59e-13 MW`，总批处理、总充电、总放电、活动场景和三类风险一致；功率平衡、SOC、包络、并网和爬坡的独立最大残差为 `1.43e-13`。逐时批处理与 BESS 充电存在等价最优分解，验证成本和碳排分别相差 0.310% 和 0.088%，故它们作为报告属性披露，不误判为约束不等价。
- 完整证据位于 `data/results/calibration/saa_master_benchmark_{scip,gurobi}/` 和 `saa_master_benchmark_summary.json`。迁移门槛通过，下一代码目标转为静态 $\Gamma$-RO；未来 RO/DRO 直接使用 Gurobi，不再新增 SCIP 实现。

## 2026-08-30：静态 Γ-RO 实现与边界预检

- `scenarios.py` 新增训练折小时位置下偏分位读取：只使用 `usable_24h_block=True` 的 2024 行，完全排除留出折，分别计算 24 个小时位置的 PV/Wind `max(0, forecast-actual)` 0.90 分位。`scheduler.py` 新增静态 Γ-RO：完整算力到达/截止包络作为有限追索索引，日前 BESS 固定，能源压力下批处理与弃电不再调整。
- 能源预算集修正为标准连续预算形式 `0<=z<=1, sum(z)<=Γ`。每条并网、弃电可用性和单步爬坡约束使用两个边际偏差的支持函数精确鲁棒化，不枚举 `2T` 压力向量，也不需要 CCG。由于每条约束最多含 PV/Wind 两项，有效预算恒为 `min(Γ,2)`；旧候选 `24/72/144/288` 全部等价，已从设计中废止。
- 新入口 `scripts/run_gamma_ro.py` 复用 SAA manifest、共同 2024 三折、`N=20` 算力支持和验证回放；默认 Gurobi，保存带源文件 SHA-256 的 `run_config.json`、逐窗口 CSV、汇总 CSV 与正式 36 窗口选择 JSON，并支持断点恢复。默认候选按 `0→0.5→0.75→1.0` 从松到紧运行；达到三类 Wilson 门槛立即停止。若首次不可行前仍未达标，则通过 `--gammas` 在独立输出目录只提交最后可行区间中点，不运行无意义大 Γ。
- 小规模测试验证支持函数在分数 Γ 下线性插值、Γ>2 饱和，以及双能源联合短缺在 Γ=2 下可正确判为不可行。真实 720 小时、20 条算力支持、`fold_1/window=0` 预检中，Γ=0、0.5、0.75 分别以 `optimal` 返回，Gurobi 时间为 11.950、13.869、16.476 秒；Γ=1、2 分别在 0.669、0.335 秒判定不可行。诊断显示最大单项风电保护为 0.17371 MW，略高于 0.17020 MW 爬坡限额，说明不可行来自边界尺度而非求解超时。
- 预检结果保存于 `data/results/calibration/gamma_ro_preflight*`，只证明实现可解及边界位于 0.75--1 之间；每个候选只有一个验证窗口，Wilson 上界不用于选参，也不得写成方法优劣结论。下一步是按自适应顺序运行完整 36 窗口 Γ-RO 三折。

## 2026-08-31：静态 Γ-RO 三折验证与参数冻结

- 正式结果目录为 `data/results/calibration/gamma_ro_three_fold/`。固定 `N=20` 算力支持、0.90 风光下偏分位、Gurobi 日前/追索、单次 300 秒时限，并在同一组 3 折×12 条30天验证窗口上完整运行正预算候选 `Γ=0.5` 与 `Γ=0.75`，共 72 条记录；所有输入文件 SHA-256 已写入 `run_config.json`。
- 两个候选均为 36/36 主问题最优。算力包络、并网和爬坡违反均为 `0/36`，三类单侧 95% Wilson 上界均为 6.99%，都达到预注册的 90% 可靠性门槛。按“最小达标正 Γ”规则冻结 `Γ=0.5`；`Γ=0.75` 仅作为进一步加严的敏感性证据，不因其样本平均实际成本略低而改选。
- `Γ=0.5/0.75` 的平均名义运行成本完全相同，均为 13,128.34 USD；平均实际运行成本分别为 14,514.00/14,483.37 USD，后者低 30.63 USD（0.211%）。该差异在36个配对窗口中22个为负、14个为正，幅度很小且可能受等价最优追索影响，不解释为更大 Γ 必然降低成本。平均主问题时间分别为 18.04/18.95 秒，`Γ=0.75` 增加约5.03%；累计求解时间为649.62/682.30秒。
- 独立审计确认72个 `(fold, window, Γ)` 键唯一，两个候选使用完全相同的36条验证轨迹；最大鲁棒约束正残差不超过 `3.28e-15 MW`，三类回放违反幅度均为数值零。`gamma_ro_selection.json` 中配置与逐窗口结果哈希重新计算一致。
- 结果只完成 Γ-RO 内部选参，尚不能比较 SAA、Γ-RO 与 TV-DRO 的方法优劣。下一步实现并校准有限支持 TV-DRO，随后在冻结参数下进行共同2025四窗口回放。
- 为避免把 `0/36` 误读为真实违反率等于0，`model.md §3.4.1`补充单侧95% Wilson score上界公式、频率学覆盖解释和三通道同时通过规则。明确 `0/1`、`0/12`、`0/36` 的上界分别为73.01%、18.40%、6.99%，而 `1/36` 为11.54%并不能通过10%门槛；该可靠性只针对2024重采样验证分布，不外推为生产SLA或2025年度概率。

## 2026-08-31：有限支持 TV-DRO 实现与三折参数冻结

- `scheduler.py` 新增有限支持TV-DRO入口。对完整联合经验支持上的二元违反事件，利用 `sup E_q[Y]=min(1,k/N+ρ)`，将每个通道的分布鲁棒机会约束精确化为 `k<=floor(N(β-ρ))`，再复用Gurobi SAA主问题、活动场景约束生成和有限批处理追索；碳排放仍只作回放属性。
- 当前 `N=20, β=0.10` 时，SAA锚点允许2个训练违反，而 `ρ=0.01/0.025/0.05` 都只允许1个。三个正半径在整数可行域上等价，因此按自适应规则先测试最小正半径0.01，通过后停止；该结果不能支持半径敏感性结论。
- `run_tv_dro.py` 固定共同manifest、三个季节平衡折、每折12条验证窗口、Gurobi日前/追索、逐窗口断点CSV、输入哈希和选择JSON。单窗口串行与4进程预检均首轮最优，4进程把报告总时间由约145.2秒降至约66.5秒，正式校准据此使用4进程。
- 正式目录 `data/results/calibration/tv_dro_three_fold/` 含36条唯一记录，每折12条，全部为 `optimal_decomposed`。算力包络、并网和爬坡均为 `0/36`，三类单侧95% Wilson上界均为6.99%，故冻结 `ρ=0.01`；平均求解时间69.30秒，平均名义/实际运行成本分别为13,128.34/14,504.94 USD。
- 独立复核确认36个 `(ρ, fold, window)` 键唯一，运行配置与逐窗口结果SHA-256均和选择文件一致。该结论只完成TV-DRO内部选参；下一步在冻结的 `N=20, Γ=0.5, ρ=0.01` 下进行共同2025回放，之后才允许比较方法表现。

## 2026-08-31：2025统一比较的确定性条件回放入口

- 为先完成改动较小且可核验的实验，新增 `load_workload_replay_scenarios`，按manifest重建100条只含算力包络的预留回放轨迹；具体2025窗口的风、光、碳残差在运行时由该窗口实际值减预测值填入，不改动原始manifest。
- 新入口 `run_2025_deterministic_workload_replay.py` 对四个2025能源窗口各求一次确定性日前计划，再在固定的该窗口实际能源轨迹下并行回放100条算力轨迹；按窗口写入断点CSV、配置和输入哈希。输出必须解释为“四条能源观测，各自含100条条件算力回放”，不能合并声称400条独立能源样本。
- 本次只实现并验证入口，不自动启动正式实验。确定性结果完成后，再以相同结果字段扩展SAA、Γ-RO和TV-DRO，避免一次引入过多新代码。

## 2026-08-31：明确任务聚合与算力包络的关系

- `compute_envelope.md §2.1` 明确：当前先计算任务core-hour，再按观测执行开始小时聚合；由于仍保留逐小时释放轨迹、固定总工作量并从同一轨迹生成 $A_t,D_t$，小时聚合不会把任务变成窗口开始时全部可执行。
- 同时登记失真边界：平均负荷/总量聚合、日级聚合、首时刻集中释放、不同真实deadline统一为单一 $H$，以及独立平滑或缩放上下包络都会改变包络含义。当前结果只代表统一反事实柔性窗口下的聚合可调度域，不是任务级生产可行域的精确恢复。

## 2026-08-31：集中整理不确定性的约束与成本解释

- 新增 `uncertainty_explained.md`，集中说明算力释放与风光误差分别进入包络、功率平衡、并网和爬坡约束，并解释这些硬约束如何通过缩小可行域间接影响运行成本。
- 文档区分名义成本、实际回放成本和鲁棒性代价，说明约束不活跃或存在等价最优解时名义成本可以不变，实际成本也不必随保守度单调变化；同时统一比较SAA、有限支持TV-DRO和静态Γ-RO的风险入口与当前结论边界。

## 2026-08-31：追索成本优先修正、结果分类与全 Gurobi 收敛

- 追索词典序由“三类运行风险→批处理 L1 调整量→总购电”改为“三类运行风险→DAM 估值购电成本→批处理 L1 调整量→总购电”。每级完成后固定上一级最优值，以确保任何成本降低都不能牺牲算力包络、并网或爬坡可靠性。当前只使用 DAM 电价评价最终购电，不虚构实时电价或两结算市场。
- 新增小规模回归用例：当两个时段均满足同一算力包络时，追索应允许偏离名义批处理计划，把 1 MWh 转移到低价时段，并得到 1 USD 的 DAM 估值购电成本。
- 代码与命令行入口已统一为 Gurobi：删除 PySCIPOpt 导入和 SCIP 追索实现，日前/追索求解器参数只接受 `gurobi`，三个可配置脚本的选项也只保留 Gurobi。项目当前运行环境同步切换为 `gurobi_env`。
- 既有 SCIP/Gurobi A/B 结果目录继续保留，只作迁移等价性和耗时证据，不是新实验入口。首次 2025 确定性回放使用旧词典序，保留在 `deterministic_2025_workload_replay/`；新脚本默认输出到 `deterministic_2025_workload_replay_cost_first/`，并在 schema 2 配置中显式记录四级词典序，防止新旧结果混写。
- 新增 `research/current_results.md` 集中分析首次 2025 回放。文档已实体分为 `docs/research/` 研究论证类和 `docs/reproduction/` 实现复现类，`docs/README.md` 保留为唯一总导航；所有仓库内相对链接随移动同步更新。同一事实只在一处详细维护，其余文档用摘要与链接导航。
- 验证证据：系统 Python 3.13.12 下 `python -m unittest discover -s tests -t .` 通过 37/37；语法编译和新词典序定向回归通过；三个命令行入口对 `scip` 均返回 argparse 错误码 2。`scip_env` 中的三个 Ridge 预测测试会以 Windows 原生错误码 `-1066598273` 退出；同一测试在系统 Python 通过，而该环境的其余主线模块通过，故定位为新安装 NumPy 2.5.1 / SciPy 1.18.1 数值库组合的环境级崩溃，未为此改动研究逻辑。
- 环境迁移验证：已存在的 `gurobi_env` 使用 Python 3.13.12、Gurobi 13.0.2、NumPy 2.2.6 和 SciPy 1.17.1；学术许可证求解测试状态为 optimal，`conda run --no-capture-output -n gurobi_env python -m unittest discover -s tests -t .` 通过 37/37。因目标名称已存在且验证通过，项目直接切换到该环境，不覆盖或删除旧 `scip_env`。

## 2026-09-01：2025统一比较、机制消融与敏感性

- 新增统一入口 `scripts/run_2025_comparison.py`。正式命令为 `python scripts/run_2025_comparison.py --experiments main ablation sensitivity --replay-workers 10 --time-limit-seconds 300 --decomposition-max-iterations 8`；固定四个2025能源窗口、20条最终训练支持、100条登记算力回放轨迹和四级追索词典序，支持只按完整100条分组断点恢复。
- 正式目录 `data/results/comparison/unified_2025/` 含主比较1600条、2×2消融1600条、敏感性2400条逐回放记录，共5600条；另含窗口/总体汇总、配对消融效应、相对基准敏感性和输入SHA-256配置。四个能源窗口是四条外部观测，每窗100条只用于条件算力风险估计。
- 主比较中四种方法的平均名义运行成本均为16,071.76 USD；平均实际成本依次为16,520.07、16,518.33、16,516.74、16,516.56 USD。三类违反在全部组均为0，弃电和求解时间的详细结果见 `current_results.md` 与CSV。因为确定性基线也为零违反且成本差小于0.03%，不声称鲁棒方法在该常规场景上具有实质优势。
- SAA外层首轮从3条活动场景开始，TV-DRO从2条开始；首轮计划在全部20条训练支持上的三类回放均通过，所以外层第1轮收敛。其余训练场景没有被忽略，而是进入全支持验收。确定性与Γ-RO没有该外层分解。
- 名义能源+名义算力消融的20条经验原子完全相同；仅在该四格中将完全相同的原子折叠为概率1的单原子，避免活动场景分解把重复副本误当作独立切割对象。任何部分重复支持均不折叠，以保留经验权重。
- 追索的二元风险选择是第一层词典序的权威判据。后续连续层可能留下约1e-6的Big-M/MIP原始残差；当第一层选择为0时，报告幅度统一置零。对应定向回归覆盖了求解返回与汇总两处口径。

## 2026-09-02：爬坡边界单因素压力

- `run_2025_comparison.py` 新增 `--ramp-limit-fraction-of-peak`，默认仍为0.10且旧 `run_config.json` 保持兼容；非默认压力目录显式记录比例。定向测试确认只改变 `r_max_mw`，不改变并网、储能、风光或算力参数。
- 依次完成0.075和0.06两档四方法×四窗口×100轨迹，共各1600条。0.075三类样本外违反均为0；0.06也均为0，但TV-DRO春季训练回放首轮爬坡3/20、第二轮2/20、第三轮0/20，活动场景从2增至4，证明约束生成已进入活跃区。
- 0.05完成确定性和SAA各四窗后，Γ-RO冬季结构性不可行并停止；独立复现为0.537秒返回 `infeasible`、`mip_gap=inf`，不是300秒时限。TV-DRO未运行，避免形成缺少Γ-RO的伪完整比较。
- 入口新增幂等 `failures.csv` 检查点及逐完整组刷新部分汇总；`summarize_2025_ramp_stress.py` 将0.10基准和三档压力统一写入 `ramp_stress_summary.csv`，明确区分 `complete`、`infeasible` 和 `not_run_after_prior_method_infeasible`。

## 2026-09-02：低成本并网—爬坡边界搜索

- 统一入口新增 `--grid-limit-fraction-of-peak` 和 `--methods`。默认并网比例仍为1.0、默认方法仍为全部注册方法，因此原统一结果配置保持兼容；非默认边界与筛选后的配置矩阵写入各自独立目录。
- 新增 `scripts/summarize_2025_boundary_search.py`，汇总方法×窗口的成本、调整、弃电、三类违反频率和幅度；确定性与TV-DRO同时存在时按相同回放ID计算Wilson区间、风险差、精确McNemar检验和预注册候选标记。
- 固定爬坡比例0.075，确定性粗筛完成0.90、0.80、0.70、0.75、0.725和0.7125共6个并网边界点。0.725及以上为零物理违反；0.7125与0.70的夏、秋均为100%并网违反，但所有已求解日前模型仍可行。风险从0%直接跳到100%，没有落入8%--20%候选区间。
- 依据首轮成本停止规则未运行TV-DRO、SAA和Γ-RO。结果保存在 `data/results/comparison/boundary_search_2025/`；后续探索性探测与首轮预注册结果分开解释。

## 2026-09-03：联合能源残差边界探测

- `run_2025_comparison.py` 新增 `--evaluation-energy-mode bootstrap_2024` 与 `--energy-residual-scale`。每条登记算力轨迹按 `energy_seed + scenario_id` 配对一条可复现的30日2024光伏—风电联合残差块自助路径；不同方法仍按相同回放ID配对。默认模式仍为实际2025能源窗口。
- 在实际2025能源路径上继续细化到0.71875、0.715625与0.7140625；风险仍在夏秋由0%直接跳到100%。0.7140625的夏、秋TV-DRO探索性探测均为0%，但确定性100%违反，不满足预注册的8%--20%候选条件。
- 联合残差发现阶段仅使用夏季名义窗口：并网比例0.85时确定性综合物理违反9/100、TV-DRO 0/100；0.86时分别为8/100和0/100，配对McNemar双侧精确检验分别为0.00390625和0.0078125。该结果用于确定后续四窗确认中心，不替代四条实际2025能源观测的正式结论。

## 2026-09-03：独立种子四窗确认

- 统一入口新增 `--energy-replay-seed`；默认仍为发现阶段冻结值20250811，非默认值只允许用于 `bootstrap_2024`，并写入 `run_config.json`。确认集冻结种子20260903，结果独立写入 `data/results/comparison/confirmation_2025/`。
- 在 $G_{max}/P_{peak}=0.85$、$R_{max}/P_{peak}=0.075$ 下完成确定性与TV-DRO四窗各100条配对回放，共800条。逐窗综合物理违反计数依次为确定性0、1、6、6，TV-DRO 0、2、0、0；夏秋各下降6个百分点，未校正McNemar双侧精确检验均为0.03125。
- 同一回放ID跨四个预测背景取“至少一次违反”后，100个聚类单元中确定性12、TV-DRO 2，风险下降10个百分点，McNemar双侧精确检验为0.00634765625。该聚类端点是避免把400行当作独立能源样本的辅助汇总，不追溯性替代逐窗门槛。
- 频率门槛通过，但严格幅度门槛未通过：最大物理违反分别为1.13808和0.90338 MW，TV/确定性比为79.38%，高于20%门槛。平均实际成本分别为16,366.52和16,361.55 USD，差额仅-4.98 USD；求解时间分别为7.21和295.06秒。因此本轮定性为部分确认，不宣称TV-DRO全面占优。
